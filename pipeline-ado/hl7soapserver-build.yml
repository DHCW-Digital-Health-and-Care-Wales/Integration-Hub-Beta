# HL7 SOAP Server Build Pipeline
trigger:
  branches:
    include:
      - main
      - release-*
  paths:
    include:
      - servers/hl7_soap_server/**
      - shared_libs/**
      - pipeline-ado/templates/**
      - ca-certs/**

pr: none

parameters:
  - name: SEMANTIC_VERSION
    displayName: 'Semantic Version'
    type: string
    default: 'v0.1.0'

variables:
  acrName: 'uksdhcwihsharedcr'
  azureServiceConnection: 'Azure - NHSWales-DHCW-IntegrationHub-Subscription'
  POOL_NAME: 'UKS-DHCW-IH-DEV-ADOManagedPool'

stages:
  - stage: CodeQuality
    displayName: 'Code Quality & Tests'
    jobs:
      - template: templates/code-quality-template.yml
        parameters:
          appName: 'servers/hl7_soap_server'
          appDisplayName: 'HL7 SOAP Server'
          pythonVersion: '3.13'
          poolName: ${{ variables.POOL_NAME }}

  - stage: BuildAndPushHL7SoapServer
    displayName: 'Build and Push HL7 SOAP Server'
    dependsOn: CodeQuality
    jobs:
      - template: templates/docker-build-push-template.yml
        parameters:
          jobName: 'BuildPushHL7SoapServer'
          displayName: 'Build and Push HL7 SOAP Server'
          appName: 'hl7soapserver'
          dockerfilePath: 'servers/hl7_soap_server/Dockerfile'
          buildContext: 'servers/hl7_soap_server'
          additionalContexts: 'shared_libs=shared_libs,ca-certs=ca-certs'
          acrName: $(acrName)
          azureServiceConnection: $(azureServiceConnection)
          POOL_NAME: ${{ variables.POOL_NAME }}
          SEMANTIC_VERSION: ${{ parameters.SEMANTIC_VERSION }}
